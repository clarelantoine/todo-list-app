import {
    AccountIcon,
    ArrowDowntIcon,
    GreetingsContainer,
} from './greetings.styles';

const Greetings = () => (
    <GreetingsContainer>
        <AccountIcon />
        <span>Hi, Clarel!</span>
        <ArrowDowntIcon />
    </GreetingsContainer>
);

export default Greetings;
